<%= form_tag add_path do %>
	<label>Product: </label>
	<%= text_field_tag :product %>
	<%= submit_tag "add to cart" %>
<% end %>

<br>

<% if session[:cart] %>
	<% session[:cart].each do |item| %>
		<%= item %><br>
	<% end %>
<% end %>